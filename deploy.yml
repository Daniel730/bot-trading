# OBSOLETE FILE
#
# The Raspberry Pi / tag-based deploy that lived here is retired.
# Production deploys are defined in:
#   .github/workflows/deploy.yml
#
# Triggers: GitHub Release published, or manual workflow_dispatch.
# Target:   self-hosted runner on bot-server (linux/amd64).
#
# This root file is intentionally not under .github/workflows/ so Actions
# will not execute it.

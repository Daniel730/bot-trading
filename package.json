{
  "name": "bot-trading-repo-tools",
  "private": true,
  "description": "Root tooling only (commitlint/husky). App deps live under frontend/.",
  "scripts": {
    "prepare": "husky || true",
    "commitlint": "commitlint --edit"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^21.2.0",
    "husky": "^9.1.7"
  }
}

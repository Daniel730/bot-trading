[pytest]
addopts = -m "not live and not benchmark and not redis_real"
markers =
    live: tests that call live external services or APIs and require explicit opt-in.
    benchmark: benchmark/load tests that are excluded from the default suite.
    redis_real: tests that require a real Redis server instead of fakes or mocks.
